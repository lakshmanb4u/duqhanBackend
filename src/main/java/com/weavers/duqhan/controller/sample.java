package com.weavers.duqhan.controller;


public class sample {
    
   /* public static void main(String[] args) throws Exception {
        
        File dir = new File("/home/mnt/dev/workspace/duqhanBackend/spellchecker/");
        
        Directory directory = FSDirectory.open(dir);
        
        SpellChecker spellChecker = new SpellChecker(directory);
        
        spellChecker.indexDictionary(
             new PlainTextDictionary(new File("/home/mnt/dev/workspace/duqhanBackend/spell.txt")));
        
        String wordForSuggestions = "hello";
        
        int suggestionsNumber = 5;

        String[] suggestions = spellChecker.
            suggestSimilar(wordForSuggestions, suggestionsNumber);

        if (suggestions!=null && suggestions.length>0) {
            for (String word : suggestions) {
                System.out.println("Did you mean:" + word);
            }
        }
        else {
            System.out.println("No suggestions found for word:"+wordForSuggestions);
        }
            
    }*/

}