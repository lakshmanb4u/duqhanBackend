package com.weavers.duqhan.controller;

import java.math.BigDecimal;
import java.math.RoundingMode;

public class sample {
    
   /* public static void main(String[] args) throws Exception {
        
    	 File dir = new File("/home/mnt/dev/workspace/duqhanBackend/spellchecker/");
        
        Directory directory = FSDirectory.open(dir);
        
        SpellChecker spellChecker = new SpellChecker(directory);
        
        spellChecker.indexDictionary(
             new PlainTextDictionary(new File("/home/mnt/dev/workspace/duqhanBackend/spell.txt")));
        
        String wordForSuggestions = "hello";
        
        int suggestionsNumber = 5;

        String[] suggestions = spellChecker.
            suggestSimilar(wordForSuggestions, suggestionsNumber);

        if (suggestions!=null && suggestions.length>0) {
            for (String word : suggestions) {
                System.out.println("Did you mean:" + word);
            }
        }
        else {
            System.out.println("No suggestions found for word:"+wordForSuggestions);
        }
    	double x= 1.2d;
    	double y= 1.6d;
    	System.out.println("="+new BigDecimal(x).setScale(1, RoundingMode.UP));
    	double d1 = roundToHalf(0);
        double d2 = roundToHalf(0);
        double d3 = roundToHalf(2.5);
        double d4 = roundToHalf(3.223920);
        double d5 = roundToHalf(3);

        System.out.println(d1);
        System.out.println(d2);
        System.out.println(d3);
        System.out.println(d4);
        System.out.println(d5);
//    	System.out.println(new BigDecimal(y).setScale(1, RoundingMode.C));
            
    }
    
    public static double roundToHalf(double d) {
    	
//    	System.out.println(Math.round(d * 2)+ " ---------- "+(d * 2)+"=== "+val);
        return (Math.ceil(d * 2)) / 2.0;
    }*/


}